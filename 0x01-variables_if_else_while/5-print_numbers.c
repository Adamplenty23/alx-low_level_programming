#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int _num = 0;

	while (_num <= 9)
	{
	printf("%d", _num);
	_num++;
	}
	putchar('\n');
	return (0);
}

