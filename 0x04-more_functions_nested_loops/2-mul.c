#include "main.h"

/**
 * mul - collects 2 digits and gives out their product
 *
 * @a: first digit to be checked
 *
 * @b: second digit to be checked
 *
 * Return: the product of the two digits
 */

int mul(int a, int b)
{
	int multiply = a * b;

	return (multiply);
}

